# Optional outside Vercel. Vercel deployments use project OIDC automatically.
AI_GATEWAY_API_KEY=

# Required together to enable the isolated browser tool.
BROWSER_PROVIDER_URL=
BROWSER_PROVIDER_TOKEN=
BROWSER_ALLOWED_DOMAINS=example.com,github.com
TOOL_APPROVAL_SECRET=

# Optional repository connectors. Status stays "Not configured" when absent.
GITHUB_TOKEN=
GITLAB_TOKEN=
